# Rust migration reconciliation handoff — 2026-09-09

## Scope and product boundary

This is an evidence checkpoint for the existing Go-to-Rust migration. It does
not move a product, repository, module, release channel, data directory, or
permission identity.

The accepted target product contract is `PB-2026-09-09` revision 2 at
`/Users/daniel/Dev/Symaira Dev/docs/product-boundaries.md`. Browse is an actively developed
optional Brain module in the target architecture, while the current
`symaira-browse` repository, `symbrowse` CLI/MCP surface, Go implementation, and
release channel remain operational until their explicit compatibility and cutover
gates pass. The repository-local product-boundary snapshot is committed on the
separate local branch `docs/product-boundaries-20260909` at
`7a3905be370d58c4de14a05732d0a84e1eed8928`; it was not treated as merged into
`main` by this checkpoint.

## Reconciled revision and evidence

The reconciled migration head is `399cead9c9688233c08495afdb49ebd53ba0bee5`
on `docs/rust-migration-prep`. Its upstream is
`origin/docs/rust-migration-prep`; it is not an integration SHA.

| Evidence | Result | Platform / source identity |
|---|---|---|
| `python3 docs/rust-port/validate.py` | pass: 88 contracts, 17 work items, acyclic DAG and valid links | macOS arm64, `399cead9` |
| `GOTOOLCHAIN=go1.26.6 CGO_ENABLED=0 make fmt-check build test lint` | pass | macOS arm64, Go reference retained |
| `cargo fmt --all --check` and `cargo check --workspace --all-targets --all-features --locked` | pass before test execution stopped | macOS arm64, Rust 1.98.0 |
| `cargo nextest run --workspace --all-features --locked` | fail | macOS arm64, `cancellation::status_failure_is_preserved_without_stopping_daemon` observed `operation_timeout` where `operation_failed` is required |
| CI workflow-dispatch run `34260695726` | fail | exact head `399cead9`; native Windows named-pipe daemon cases fail, plus other non-green migration gates |

The Windows daemon failure is real runtime evidence, not a cross-compile
substitute. The failed cases are
`delayed_fragmented_frame_is_served_after_accept`,
`stalled_fragmented_frame_times_out_and_closes_connection`, and
`concurrent_starts_have_one_owner_and_recover_after_stop`; their clients receive
`NotFound` rather than observing a published pipe. The complete RUST gate chain
was therefore not executed past its first failing `nextest` gate.

## Current matrix and work-item state

The contract matrix at this revision contains 88 rows: 48 `parity`, 14
`fixture-ready`, 2 `accepted-difference`, and 24 `todo`. A fixture-ready row is
not a completed behavioral replacement.

`RUST-006` is corrected to `in_progress`: its daemon lifecycle work must not be
called complete while the native Windows evidence and the current status-error
regression are red. `RUST-012` and `RUST-013` remain in progress; `RUST-017`,
`RUST-015`, and `RUST-016` remain blocked. The hard value gate is also red:
recorded paired fetch p95 is 30.326 ms in Rust versus 5.894 ms in Go, above the
permitted 6.4834 ms ceiling. The compatibility transport decision is also
unresolved for full cutover: the Rust candidate did not reproduce the six legacy
browser-profile wire captures, so the explicitly named Go compatibility path
remains necessary.

## Boundaries and direct consumers to preserve

| Boundary | Current public / operational entrypoint | Data and permission ownership |
|---|---|---|
| CLI | `symbrowse`, including nested Cobra command/flag behavior and exit/stderr contracts | XDG/TOML config and local state remain Browse-owned |
| MCP | `symbrowse mcp`, stdio JSON-RPC only | stdout is protocol-only; daemon and policy failures are typed and redacted |
| daemon | `symbrowse daemon`, local newline-delimited protocol | session socket/pipe lifecycle, session state, policy and browser child ownership remain daemon-owned |
| engines | static HTTP; explicitly selected Chrome, Safari, Firefox, or `compat` | browser profiles/sessions are separate from long-lived agent memory; native browser permissions remain adapter-specific |
| in-process Go API | `formflow` package | retain while external consumers or a separately versioned language-neutral replacement remain unresolved |

The Rust workspace mirrors those boundaries through `symbrowse-cli`, `-mcp`,
`-daemon`, `-core`, `-engine*`, `-fetch`, and `-compat`. It has no compile-time
imports of sibling Symaira repositories. No native Swift component is in this
migration scope. The package/release source remains the Go pipeline until an
explicit RUST-016 decision.

## Reproduction and rollback

The executable reference is pinned at Go commit
`652453d1595fc302bd69c328e7da8a21dbee28b9` / release `v0.8.0` with Go 1.26.6:

```text
GOTOOLCHAIN=go1.26.6 CGO_ENABLED=0 make port-oracle-build
make differential-go-selftest
```

The Go binary remains the release default and rollback path. Do not remove it,
retag it, or point ordinary releases at Rust. The Rust dual-artifact selector
and rollback model are recorded in `port/release/dual-release-manifest.json`
and `port/release/rollback-matrix.json`; release rehearsal remains blocked until
all native, performance, signature and package gates are genuinely green.

## Required continuation

1. Repair the RUST-006 native named-pipe startup/lifecycle path and status-error
   regression; then run the native Windows daemon suite on the exact candidate.
2. Re-run the complete portable Rust gates from a clean, source-identified
   candidate; do not inherit a prior green subset.
3. Complete native Chrome/Safari/Firefox evidence and the explicit
   multi-browser/compat boundary without silent fallback.
4. Profile and repair the independently gated static-fetch p95 regression.
5. Only after all contract rows, hardening, native release artifacts and the
   reversible rollback rehearsal pass may RUST-016 decide whether Rust can
   become a default.

## Worktrees at checkpoint

The coordinator checkout is `docs/rust-migration-prep`. Preserved associated
worktrees include `rust/chrome-daemon-evidence` (draft PR #421),
`fix/pinned-go-oracle-build`, the separate unmerged
`docs/product-boundaries-20260909` snapshot, and existing subagent worktrees.
They are not cleaned up or incorporated by this checkpoint.
